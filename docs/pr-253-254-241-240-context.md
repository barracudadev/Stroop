# Context Notes — PR covering #253, #254, #241, #240

> Working notes only. Not real repo state — written to compare against
> the actual PR/diff once it exists.

## Repo assumptions
- Repo: barracudadev/Stroop (monorepo, per issue
  text: "Review the related service area in the monorepo").
- Assumed frontend framework/test tooling unknown — issues don't
  specify (React? Vue? Which e2e tool — Playwright, Cypress? Which
  component test tool — Testing Library, Vitest, Jest?).
- All four issues share an identical template ("Suggested implementation
  notes": review monorepo area, define acceptance criteria, add/update
  tests and docs) — meaning none of them specify concrete acceptance
  criteria themselves. Everything below is inferred from the title +
  one-line description only.

## Issue #253 — E2E tests for dashboard navigation
- Assumed "main user journeys" means: landing page -> dashboard home
  -> at least one analytics view -> back navigation.
- Unknown: exact routes/pages that exist today, and which e2e
  framework is already configured (if any).

## Issue #254 — Component testing for shared UI elements
- Assumed "shared UI elements" = a `components/` or `shared/` directory
  with reusable chart, card, and filter components used across
  multiple dashboard views.
- Unknown: existing test coverage baseline, snapshot vs. behavioral
  testing convention used in this repo.

## Issue #241 — Improve performance of large chart rendering
- Assumed the chart library in use is unknown (Recharts? D3? Chart.js?)
  — this materially changes what "optimize" means (virtualization,
  memoization, canvas vs. SVG, downsampling).
- Assumed "many data points or many panels" implies both a
  single-large-dataset case and a many-small-charts-on-one-page case.
- Unknown: any existing perf budget/benchmark to compare against.

## Issue #240 — User preferences for default time range
- Assumed preferences persist via browser storage (localStorage) or
  a backend user-settings endpoint — unknown which, and issue doesn't
  say if this repo has user accounts/auth at all.
- Assumed "dashboard views and filters" beyond time range are out of
  scope for this issue (title only mentions "default time range";
  description says "views and filters" more broadly — possible scope
  mismatch worth confirming against the real issue thread).

## Biggest risk to the guessed PR diverging from reality
1. Which chart/e2e/test libraries are actually in use — this affects
   every file path and API in the diff.
2. Whether preferences are persisted client-side or server-side
   (issue #240) — very different implementation shapes.
3. Scope mismatch in #240 (title says "time range," description says
   "views and filters").

## To fill in once you have real repo access
- [ ] Confirm frontend framework and existing test tooling (e2e + unit/component)
- [ ] Confirm chart library used for dashboard visualizations
- [ ] Confirm whether user accounts/auth exist (relevant to #240)
- [ ] Confirm existing routes/pages for e2e navigation coverage (#253)
- [ ] Confirm actual scope of #240 (time range only vs. views + filters)